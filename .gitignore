# ---- Secrets / env ----------------------------------------------------------
.env
*.local
!.env.example

# ---- Go build artifacts -----------------------------------------------------
bin/
*.exe
*.test
*.out
coverage.*

# ---- OS / editor ------------------------------------------------------------
.DS_Store
*~
.idea/
.vscode/
*.swp

# ---- Legacy / archived material (kept locally, out of the tracked project) --
.archive/
Archive/

# ---- Load-test scratch ------------------------------------------------------
loadtest/*.tmp
*.k6-summary.json
# Generated run reports (run.sh / k6.sh write here) — keep the dir, ignore outputs
deploy/loadtest/reports/*.md
!deploy/loadtest/reports/.gitkeep

# ---- DB migration dumps (generated artifacts) ----
db/migration/
