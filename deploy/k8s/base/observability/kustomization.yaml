apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

resources:
  - namespace.yaml
  - otel-collector.yaml
  - tempo.yaml
  - prometheus.yaml
  - grafana.yaml
  - loki.yaml
